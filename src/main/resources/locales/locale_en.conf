prefix = "<color:#f5a5a5><bold>F <dark_gray>»<reset>"

feedback {
    generate = "<prefix> Given generated feather for duration <duration>"

    status = "<prefix> You have <duration> remaining"

    redeemed = "<prefix> Redeemed <duration> of flight"

    enable = "<prefix> <green>Enabled flight"
    already-enabled = "<prefix> Flight already enabled"
    no-flight-remaining = "<prefix> <red>Cannot enable flight because you have no time remaining"

    disable = "<prefix> <green>Disabled flight"
    already-disabled = "<prefix> <red>Flight already disabled"
}
